<div class="row">
    <div class="col-12 p-3">
        <div class="row">
            {% for item in ['label', 'domain', 'locale', 'code'] %}
                <div class="col-12">
                    {{ form_row(form[item]) }}
                </div>
            {% endfor %}
        </div>
    </div>
</div>

